📘 syntax: CleanAllOutputs.fn.sh
📘 syntax: CleanAllOutputs.fn.sh [--help|--help-syntax]

##  Summary:

		Performs full workspace cleanup of generated artifacts and caches before a clean
		rebuild.

##  Arguments:

		None. This command accepts no positional arguments.

##  Options:

		--help
			Prints command help and exits.

		--help-syntax
			Prints syntax summary and exits.

##  Examples:

		# Clean all generated artifacts and caches
		`CleanAllOutputs.fn.sh`
